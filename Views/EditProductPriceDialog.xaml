<Window x:Class="MenuStolovaya.Views.EditProductPriceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Изменение цены продукта" 
        Height="450" 
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                   Text="Изменение цены продукта"
                   Style="{StaticResource SubtitleStyle}"
                   Margin="0,0,0,15"/>

        <!-- Основная форма -->
        <Border Grid.Row="1" 
                Style="{StaticResource CardStyle}">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Наименование продукта -->
                <TextBlock Grid.Row="0" Grid.Column="0" 
                           Text="Наименование:"
                           VerticalAlignment="Center"
                           Margin="0,5"/>
                <TextBlock x:Name="ProductNameText" 
                           Grid.Row="0" Grid.Column="1" 
                           VerticalAlignment="Center"
                           Margin="5"
                           FontWeight="Bold"/>

                <!-- Артикул -->
                <TextBlock Grid.Row="1" Grid.Column="0" 
                           Text="Артикул:"
                           VerticalAlignment="Center"
                           Margin="0,5"/>
                <TextBlock x:Name="ArticleText" 
                           Grid.Row="1" Grid.Column="1" 
                           VerticalAlignment="Center"
                           Margin="5"/>

                <!-- Категория -->
                <TextBlock Grid.Row="2" Grid.Column="0" 
                           Text="Категория:"
                           VerticalAlignment="Center"
                           Margin="0,5"/>
                <TextBlock x:Name="CategoryText" 
                           Grid.Row="2" Grid.Column="1" 
                           VerticalAlignment="Center"
                           Margin="5"/>

                <!-- Единица измерения -->
                <TextBlock Grid.Row="3" Grid.Column="0" 
                           Text="Единица измерения:"
                           VerticalAlignment="Center"
                           Margin="0,5"/>
                <TextBlock x:Name="UnitText" 
                           Grid.Row="3" Grid.Column="1" 
                           VerticalAlignment="Center"
                           Margin="5"/>

                <!-- Текущая цена -->
                <TextBlock Grid.Row="4" Grid.Column="0" 
                           Text="Текущая цена:"
                           VerticalAlignment="Center"
                           Margin="0,5"/>
                <TextBlock x:Name="CurrentPriceText" 
                           Grid.Row="4" Grid.Column="1" 
                           VerticalAlignment="Center"
                           Margin="5"
                           FontWeight="SemiBold"
                           Foreground="#FF2196F3"/>

                <!-- Новая цена -->
                <TextBlock Grid.Row="5" Grid.Column="0" 
                           Text="Новая цена:"
                           VerticalAlignment="Center"
                           Margin="0,5"/>
                <Grid Grid.Row="5" Grid.Column="1" Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="NewPriceTextBox" 
                             Grid.Column="0"
                             Style="{StaticResource ModernTextBoxStyle}"
                             PreviewTextInput="PriceTextBox_PreviewTextInput"
                             TextChanged="NewPriceTextBox_TextChanged"/>
                    <TextBlock Grid.Column="1" 
                               Text="руб."
                               VerticalAlignment="Center"
                               Margin="10,0,0,0"/>
                </Grid>

                <!-- Изменение цены -->
                <TextBlock Grid.Row="6" Grid.Column="0" 
                           Text="Изменение:"
                           VerticalAlignment="Center"
                           Margin="0,5"/>
                <TextBlock x:Name="ChangeText" 
                           Grid.Row="6" Grid.Column="1" 
                           VerticalAlignment="Center"
                           Margin="5"
                           FontWeight="SemiBold"/>
            </Grid>
        </Border>

        <!-- Кнопки -->
        <Border Grid.Row="2" 
                Style="{StaticResource CardStyle}" 
                Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Right">
                <Button Content="Сохранить" 
                        Style="{StaticResource PrimaryButtonStyle}"
                        Width="100"
                        Click="SaveButton_Click"
                        Margin="5"/>
                <Button Content="Отмена" 
                        Style="{StaticResource PrimaryButtonStyle}"
                        Background="{StaticResource AccentDarkBrush}"
                        Width="100"
                        Click="CancelButton_Click"
                        Margin="5"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>